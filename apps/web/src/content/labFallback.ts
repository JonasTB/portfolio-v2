import type { LabExperiment } from '@portfolio/contracts';

/**
 * Cópia local dos experimentos do Lab (mesma fonte que
 * apps/api/src/content/lab.ts). Vazio por decisão — ver comentário lá.
 */
export const labFallback: LabExperiment[] = [];
